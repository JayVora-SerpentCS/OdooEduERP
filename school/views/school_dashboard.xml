<odoo>

    <!-- KanBan View Of DashBoard Event -->

    <record id="student_dashboard_kanban_view" model="ir.ui.view">
        <field name="name">student.dashboard.dashboard</field>
        <field name="model">student.dashboard</field>
        <field name="arch" type="xml">
             <school_dashboard_view class="oe_background_grey o_kanban_dashboard o_school_kanban" create="0"> 
                <field name="name"/>
                <!-- <templates>
                    <t t-name="kanban-box">
                    </t>
                </templates> -->
            </school_dashboard_view> 
        </field>
    </record>

</odoo>